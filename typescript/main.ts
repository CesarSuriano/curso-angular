var variavel = "olaaaa"

function funcao(x, y){
    return x + y;
}

//ES6 ou 2015
let num = 2
const PI = 3.14

var n1 : String = "sdfa"
n1 = 3;

var numeros = [1,2,3]

numeros.map(function(valor) {
    return valor * 2
});

numeros.map(valor => valor * 2)

class Matematica {
    soma(x, y) {
        return x + y
    }
}

